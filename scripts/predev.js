const yellowColorCode = "\x1b[33m";
const whiteColorCode = "\x1b[0m";

console.log(`${yellowColorCode}
    Using developer mode is only recommended for developers, please use "dist" mode for production.
    And run the production build using "start" scripts ${whiteColorCode}
`);